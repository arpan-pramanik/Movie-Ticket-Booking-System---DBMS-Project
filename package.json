{
    "name": "movie-ticket-booking-system",
    "version": "1.0.0",
    "description": "Movie Ticket Booking System - Full Stack",
    "scripts": {
        "dev": "concurrently \"npm run server\" \"npm run client\"",
        "server": "cd server && node app.js",
        "client": "cd client && npm run dev",
        "install:all": "cd server && npm install && cd ../client && npm install",
        "db:setup": "mysql -u arpan -parpan < server/schema.sql && mysql -u arpan -parpan movie_booking < server/seed.sql"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}